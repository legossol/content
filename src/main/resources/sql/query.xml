<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dev">
    <select id="getMyData" resultType="java.util.HashMap">
        select *
        from photo
        limit 1
    </select>
</mapper>

